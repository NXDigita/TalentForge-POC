{
  "name": "talentforge-worker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "ts-node-dev --clear --respawn --transpile-only src/index.ts",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "bullmq": "^5.80.2",
    "ioredis": "^5.3.2"
  },
  "devDependencies": {
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.6.2"
  }
}